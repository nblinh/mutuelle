IRMA Documentation1.	Import database Postgresql :-	Create database « postgres » with user « postgres »-	Login to database with user « postgres »-	Import database by command : \i C:\IRMA\dump_20181031_0618.sql2.	Config and run IRMA python code without Docker-	Go to irma/api/config/develoment.py, change the configuration :DATABASE_HOST = "127.0.0.1"DATABASE_NAME = "postgres"DATABASE_PASSWORD = "admin"DATABASE_USER = "postgres"-	Set the path config to this file :set CONFIGURATION_FILE=C:\IRMA\irma\api\config\development.pyset FLASK_APP=main.py-	Install all the packages in pipfile : Irma/api/Pipfile :python3 -m pip install --proxy=http://username:password@10.119.1.111:8080 click psycopg2-binary psycopg2 pytest mock…-	Run application : python3 -m flask run3.	Run IRMA react code without Docker (just for developpment)-	set process.env.REACT_APP_API_URL to python webservice or go to irma\front\services\request change baseURL to web service adresse. For example: baseURL: "http://127.0.0.1:5000",-	set process.env.REACT_APP_STANDALONE = true or go to irma\front\src\index.js change the code to ://if (process.env.REACT_APP_STANDALONE) {    const registerServiceWorker = require('./registerServiceWorker').default;    ReactDOM.render(<App />, document.getElementById('root'));    registerServiceWorker();// } else {//     global.bootstrapIrma = element => {//         ReactDOM.render(<App />, element);//     };// }-	Install the libs : npm install-	Run the code : npm start4.	Compile and run web service with Java code (equals to python web services)-	To compile java code, go to root directory where pom.xml located. Run: mvn clean install-	To run with eclipse : run main class : IrmaApplication.java-	To run spring boot application deploy, go to irma\target run : java -jar irma-0.0.1-SNAPSHOT.jar-	To show Api Documentation with swagger, go to http://localhost:8080/swagger-ui.html